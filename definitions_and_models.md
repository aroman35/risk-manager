
# Справочник обозначений и используемых моделей

## 📊 1. Переменные временного ряда

| Обозначение | Полное название | Что это значит | Как используется |
|--------------|----------------|----------------|-------------------|
| $P_t$ | **Price** — цена актива в момент времени $t$ | Рыночная цена на бирже в момент времени $t$ (последняя цена сделки или mid-price) | Исходные данные, из которых строятся доходности и спрэды |
| $r_t = \ln(P_t / P_{t-1})$ | **Логарифмическая доходность** | Относительное изменение цены за один шаг времени | Используется в моделях волатильности (GARCH, EGARCH) |
| $x_t$ | **Спрэд / ошибка коинтеграции** | Разница между ценами двух активов или отклонение текущей цены от равновесия $\mu$ | Главный процесс в MR-модели (anomaly signal) |
| $Z_t$ | **Набор регрессоров (features)** | Вектор факторов: спред бид-аск, ликвидность, объём, фандинг, волатильность, BTC.D, DXY и т.д. | Вводится в регрессию MR для адаптации к рыночным условиям |

---

## ⚙️ 2. Модели динамики цены

### 📈 AR(1) — авторегрессия первого порядка
$$
x_t = \mu + \phi (x_{t-1} - \mu) + \varepsilon_t, \quad |\phi| < 1
$$

- Простая форма mean-reversion (возврат к среднему).  
- $\phi$ определяет **скорость возврата**, half-life:  
$$
HL = \frac{\ln(2)}{-\ln(\phi)}
$$

---

### 🌀 OU — процесс Орнштейна–Уленбека (непрерывный аналог AR(1))
$$
dx_t = \kappa(\mu - x_t)dt + \sigma_t dW_t
$$
- $\kappa$ — скорость возврата (аналог $1 - \phi$);  
- $\mu$ — равновесный уровень;  
- $\sigma_t$ — волатильность;  
- $dW_t$ — винеровский процесс.  

Используется для описания плавного возврата к среднему в непрерывном времени (спрэды, процентные ставки).

---

## ⚡ 3. Модели условной волатильности

### GARCH (Generalized AutoRegressive Conditional Heteroskedasticity)
$$
\sigma_t^2 = \omega + \alpha \varepsilon_{t-1}^2 + \beta \sigma_{t-1}^2
$$
- Моделирует **кластеризацию волатильности** — периоды спокойствия и турбулентности.  
- $\alpha$ — реакция на новые шоки, $\beta$ — инерция, $\omega$ — базовый уровень.  
- Используется для стандартизации остатков: $z_t = \varepsilon_t / \sigma_t$.

---

### EGARCH (Exponential GARCH)
$$
\ln(\sigma_t^2) = \omega + \beta \ln(\sigma_{t-1}^2) + \alpha |z_{t-1}| + \gamma z_{t-1}
$$
- Учитывает **асимметрию** — отрицательные шоки увеличивают волатильность сильнее, чем положительные.  
- Применяется при скошенных распределениях доходностей.

---

## 🧮 4. EVT и хвостовые распределения

### POT (Peaks Over Threshold)
Метод EVT, который рассматривает **все наблюдения выше порога $u$**.  
Превышения $Y = X - u$ описываются **GPD**.

### GPD (Generalized Pareto Distribution)
$$
P(Y \le y) = 1 - \left(1 + \xi \frac{y}{\beta}\right)^{-1/\xi}, \quad y > 0
$$
- $\xi$ — **shape (tail index)**, показывает “толщину” хвоста.  
  - $\xi > 0$ — heavy-tailed,  
  - $\xi = 0$ — экспоненциальный хвост.  
- $\beta$ — **scale** параметр.  
- Используется для оценки **квантилей** и **ES (Expected Shortfall)** в хвостах:  
$$
q_\alpha = u + \frac{\beta}{\xi}\left[\left(\frac{1-\alpha}{p_u}\right)^{-\xi}-1\right]
$$

---

## 🔗 5. Общая цепочка анализа

1. Из TimescaleDB берём $P_t$ → считаем $r_t$, формируем $x_t$, $Z_t$.  
2. AR(1)/OU → оцениваем скорость mean-reversion.  
3. GARCH/EGARCH → получаем условную волатильность $\sigma_t$.  
4. EVT (POT/GPD) → оцениваем хвостовые параметры $(\xi, \beta)$ и режим риска.  
5. VaR/CVaR → вычисляем лимиты для исполнения стратегии.

---

📚 **Основные источники:**  
Engle (1982), Bollerslev (1986), Tsay (Wiley), McNeil–Frey–Embrechts (2015), Coles (2001), Cont (2001), Stefan Jansen, Raja Velu, Maxence Hardy, Daniel Nehren, Steve Bell.
